# Cloudflare Pages — biz.fyndroo.com
# Deploy: ./scripts/deploy_cdn.sh
# Uses CLOUDFLARE_ACCOUNT_ID env (Koshy.vibin team) — see deploy_cdn.sh

name = "fyndroo-biz"
compatibility_date = "2024-01-01"
pages_build_output_dir = "dist"
